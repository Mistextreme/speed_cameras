# [1]. To create your own language, you need to use an existing template, such as notifications_en.lua. In Config.lua, you only need to specify the _xx.lua suffix that represents your language code. Stop the resource, then restart it.